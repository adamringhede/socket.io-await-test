{
  "name": "socket.io-await-test",
  "version": "1.0.0",
  "description": "A tool to test socket io servers more easily without dealing with callbacks",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc && mocha --reporter spec",
    "prepublish": "tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.0",
    "mocha": "^8.3.0",
    "socket.io-client": "^3.1.1",
    "tslint": "^6.1.3",
    "typescript": "^4.1.5",
    "socket.io": "^3.1.1"
  },
  "dependencies": {
  }
}
